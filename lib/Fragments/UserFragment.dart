import 'package:flutter/cupertino.dart';

class UserFragement extends StatelessWidget {
  const UserFragement({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('This is User'),
      ),
    );
  }
}
