import 'package:flutter/cupertino.dart';

class WalletFragement extends StatelessWidget {
  const WalletFragement({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('This is Wallet'),
      ),
    );
  }
}
