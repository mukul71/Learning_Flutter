import 'package:flutter/cupertino.dart';

class MailFragement extends StatelessWidget {
  const MailFragement({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('This is Mail'),
      ),
    );
  }
}
