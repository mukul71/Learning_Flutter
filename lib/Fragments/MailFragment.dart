import 'package:flutter/cupertino.dart';

class MailFragment extends StatelessWidget {
  const MailFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Mail Fragment'),
      ),
    );
  }
}
