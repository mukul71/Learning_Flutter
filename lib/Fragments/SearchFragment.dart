import 'package:flutter/cupertino.dart';

class SearchFragement extends StatelessWidget {
  const SearchFragement({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('This is Search'),
      ),
    );
  }
}
