import 'package:flutter/cupertino.dart';

class CardFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Card Fragment'),
      ),
    );
  }
}
